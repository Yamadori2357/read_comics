<% provide(:title, "ホーム") %>
<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title) %></title>
  </head>
<% if logged_in? %>
      <section class="micropost_form">
        <%= render 'shared/micropost_form' %>
      </section>
    <div class="col-md-8">
      <%= render 'shared/feed' %>
    </div>
<% else %>
<% end %>
</html>